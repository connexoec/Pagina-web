node_modules
dist
dist-ssr
*.local

# Variables de entorno (webhooks de Discord, claves de Supabase). NUNCA al repo.
# El archivo versionado es `.env.example` (solo placeholders).
.env
.env.*
!.env.example
.DS_Store
.vscode/*
!.vscode/extensions.json
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Assets fuente sin usar + capturas de dev (no van a producción)
/Perfiles/
connexo-dev-screenshot.png
.vercel

# CONFIDENCIAL — material interno de Connexo. NUNCA al repositorio.
# El manual de capacitación contiene rutas del panel /admin, roles, nombres de
# scripts SQL de Supabase y el flujo de credenciales de pasarela de pago.
/Documentos/
*.docx
